session.use_only_cookies = 0
session.use_only_cookies = 1

session.cookie_httponly=false  ; Noncompliant {{Set the "session.cookie_httponly" property to "true" if needed.}}
session.cookie_httponly=0  ; Noncompliant {{Set the "session.cookie_httponly" property to "true" if needed.}}

session.cookie_httponly=  ; Noncompliant {{Set the "session.cookie_httponly" property to "true" if needed.}}
