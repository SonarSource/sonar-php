session.cookie_httponly = 0
session.cookie_httponly = 1

session.cookie_secure=false  ; Noncompliant {{Make sure creating the session cookie without the "secure" flag is safe here.}}
session.cookie_secure=0  ; Noncompliant {{Make sure creating the session cookie without the "secure" flag is safe here.}}

session.cookie_secure=  ; Noncompliant {{Make sure creating the session cookie without the "secure" flag is safe here.}}

session.cookie_secure=true ; Compliant
